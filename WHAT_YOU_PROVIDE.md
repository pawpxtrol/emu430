# One-page checklist — what only you can provide

Use this with `README.md` in the same folder. For a **PDF-by-PDF audit**, open [`COURSE_COMPLIANCE.md`](COURSE_COMPLIANCE.md).

1. **Exact download URL** for the Excel file (portal or press release) + **date accessed** → paste into `data.qmd`.
2. **GitHub:** account **`pawpatrol`** — `site-url` / `repo-url` in `_quarto.yml` point to **`https://github.com/pawpatrol/emu430`** and **`https://pawpatrol.github.io/emu430/`**. Create the **`emu430`** repo on GitHub if missing. Change URLs if Classroom used a different path. Confirm with the instructor whether the **Classroom team name** must match the official list while **“Paw Patrol”** is the project title on the site.
3. **Every teammate’s name** and **GitHub profile link** (from Assignment 1) → `index.qmd` table.
4. **Link from each personal coursework page** back to the **published** project URL.
5. **HADI PDF**: render per course instructions and upload yourself (not automated here).
6. **Presentation**: rehearse using the **live website** (no PowerPoint per course).

Optional: if you use AI for prose, **highlight** those paragraphs and add **footnotes with prompts**; re-check numbers against `feeding`.
